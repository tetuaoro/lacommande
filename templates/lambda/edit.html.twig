{% extends 'base.html.twig' %}

{% block title %}Edit Lambda{% endblock %}

{% block body %}
    <h1>Edit Lambda</h1>

    {{ include('lambda/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('lambda_index') }}">back to list</a>

    {{ include('lambda/_delete_form.html.twig') }}
{% endblock %}
