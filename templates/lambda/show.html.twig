{% extends 'base.html.twig' %}

{% block title %}Lambda{% endblock %}

{% block body %}
    <h1>Lambda</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lambda.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lambda.name }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ lambda.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ lambda.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ lambda.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lambda_index') }}">back to list</a>

    <a href="{{ path('lambda_edit', {'id': lambda.id}) }}">edit</a>

    {{ include('lambda/_delete_form.html.twig') }}
{% endblock %}
