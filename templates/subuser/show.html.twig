{% extends 'base.html.twig' %}

{% block title %}Subuser{% endblock %}

{% block body %}
    <h1>Subuser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subuser.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ subuser.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('subuser_index') }}">back to list</a>

    <a href="{{ path('subuser_edit', {'id': subuser.id}) }}">edit</a>

    {{ include('subuser/_delete_form.html.twig') }}
{% endblock %}
