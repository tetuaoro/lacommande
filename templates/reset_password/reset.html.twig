{% extends 'base.html.twig' %}

{% block title %}
	{{"Rénitialiser le mot de passe" | trans}}
{% endblock %}

{% block body %}

	<div class="reset-password p-t-130 p-b-95">
		<div class="row">
			<div class="col-md"></div>
			<div class="col-md">
				{{ form_start(resetForm) }}
				{{ form_row(resetForm.plainPassword) }}
				<button class="btn btn-primary">{{"Rénitialiser" | trans}}</button>
				{{ form_end(resetForm) }}
			</div>
			<div class="col-md"></div>
		</div>
	</div>

{% endblock %}
